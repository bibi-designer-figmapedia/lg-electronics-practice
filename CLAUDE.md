# 디자인 시스템 하네스

Figma 디자인 시스템을 코드로 변환하는 하네스. 이 문서 최상단의 **목적 3가지와 완료 기준**이 이 저장소의 판정 기준이다. 아래 기준을 만족하지 못한 작업은 "완료"가 아니다.

## 스택

Vite 6 · React 19 · TypeScript 5 · Tailwind CSS v4 · Storybook 8 (addon-designs + autodocs)

---

## 목적 1 — 일관성 보장

**모든 시각 값이 token만 참조한다.**

`src/**/*.{ts,tsx,css}` 전체에서 아래 항목이 **전부 0건**일 때 pass:

| 위반 항목 | 예시 |
| --- | --- |
| raw hex | `#fff`, `#0a0a0a` |
| `rgb()` / `rgba()` / `hsl()` 리터럴 | `rgba(0,0,0,.5)` |
| px / rem 리터럴 (spacing · radius · font-size · border-width) | `padding: 13px`, `border-radius: .5rem` |
| Tailwind arbitrary value (대괄호 문법) | `p-[13px]`, `text-[#ff0000]`, `w-[347px]` |
| 인라인 `style={{ }}` 에 들어간 시각 값 | `style={{ marginTop: 8 }}` |

- **예외**: token을 **정의**하는 위치(예정: `src/tokens/`) 단 1곳만 raw 값을 가질 수 있다.
- **그 외 예외를 인정받는 유일한 경로**: (a) 코드에 사유 주석 + (b) 이 문서의 예외 목록 등재. 둘 중 하나만 있으면 fail.
- **검증**: `npm run verify:tokens` → exit code 0

### 예외 목록

_(현재 없음)_

---

## 목적 2 — 하드코딩 자동 차단

**token 외 값이 들어오면 사람 리뷰가 아니라 도구 레벨에서 자동 차단된다.**

pass 조건:

- 위반이 포함된 편집은 **파일에 기록되기 전에** 차단된다. 기록 후 경고는 fail.
- 차단 주체는 hook이다. Claude가 "다음엔 조심하겠다"고 말하는 것은 pass가 아니다.
- 차단 메시지에 **(a) 위반한 값**과 **(b) 대신 써야 할 token 이름**이 함께 나온다.
- 로컬과 CI가 같은 판정을 낸다 — `npm run verify:tokens`의 결과가 양쪽에서 동일하다.

**판정 실험**: 일부러 `className="text-[#ff0000]"` 를 넣은 편집을 시도한다. 차단되면 pass, 파일에 남으면 fail.

---

## 목적 3 — 구현 프로세스 표준화

**모든 컴포넌트 작업이 동일한 4단계 절차를 거친다.**

컴포넌트 1개가 "완료"이려면 아래 4개 산출물이 **모두** 존재해야 한다:

1. **Figma 원본 참조** — node 링크 또는 Code Connect 매핑이 기록됨
2. **token 매핑** — Figma 변수 → 코드 token 이름 대응이 기록됨
3. **구현체** — `*.tsx`
4. **Story** — `*.stories.tsx`. addon-designs의 `design` parameter로 Figma 링크가 연결되고 autodocs 페이지가 생성됨

- 4개 중 하나라도 없으면 그 컴포넌트는 **미완료**로 계산한다. 3/4는 완료가 아니다.
- **검증**: `npm run verify:process` → 컴포넌트별 4단계 체크 표를 출력, 미충족이 하나라도 있으면 exit code 1
- **이 검증이 판정하지 못하는 것**: 산출물 1·2는 링크와 대응표가 **기록됐는지**만 본다. 기록된 매핑이 실제 Figma와 맞는지는 판정 대상이 아니다 — 그것은 `design-reviewer`의 몫이다. 즉 `verify:process` 통과는 "4단계를 거쳤다"는 증거이지 "구현이 정확하다"는 증거가 아니다.

> 4단계 각각의 *내용*(4원칙)은 아래 `## 카파시 4원칙`과 `## 표준 워크플로` 절에서 정의된다. 이 절은 "4단계를 통과해야 완료"라는 **판정 규칙**만 고정한다.

---

## 카파시 4원칙

각 원칙은 **막는 문제 + 강제 방식**의 쌍이다. 슬로건은 위반을 판정할 수 없으므로 쓰지 않는다.

### 1. Think Before Coding

- **막는 문제**: 잘못된 가정 — 확인하지 않은 전제 위에 코드를 쌓는다. Figma 값·token 이름·컴포넌트 존재 여부를 짐작하고 진행하면, 틀린 지점이 구현 끝에서야 드러난다.
- **강제 방식**: 모호하면 **멈추고 질문한다**. 추측을 사실처럼 말하지 않는다 — 확인한 것과 추정한 것을 문장에서 구분한다.
- **위반 신호**: "아마 ~일 것", "보통 ~로 한다"가 구현 근거로 등장한다. 확인 없이 token 이름을 지어낸다.

### 2. Simplicity First

- **막는 문제**: 부풀리기 — 이미 있는 token·컴포넌트를 놔두고 새로 만들고, 요청되지 않은 추상화·prop·옵션을 얹는다. 디자인 시스템에서 중복 정의는 곧 불일치다.
- **강제 방식**: 새로 만들기 전에 **기존 token·컴포넌트를 먼저 조사해 재사용한다**. 요청하지 않은 추상화·옵션·variant는 만들지 않는다.
- **위반 신호**: 기존 token을 찾아본 기록 없이 새 token을 추가한다. "나중에 쓸 것 같아서" 추가된 prop이 story에서 쓰이지 않는다.

### 3. Surgical Changes

- **막는 문제**: 범위 밖 변경 — 요청과 무관한 리팩터링·포맷팅·이름 변경이 섞여 들어가, 무엇이 요청이고 무엇이 부작용인지 구분할 수 없게 된다.
- **강제 방식**: **변경된 모든 줄이 요청과 1:1로 추적**되어야 한다. 어떤 줄을 왜 바꿨는지 요청 문장으로 지목할 수 없으면 그 줄은 되돌린다.
- **위반 신호**: 요청과 무관한 파일이 변경 목록에 있다. "지나가다 정리했다"는 설명이 붙은 변경이 있다.

### 4. Goal-Driven Execution

- **막는 문제**: 미완 종료 — 쉬운 부분만 하고 "완료"라 보고한다. 완료 조건이 없으면 언제 끝났는지 판정할 수 없어, 미완이 완료로 통과한다.
- **강제 방식**: 착수 전에 **측정 가능한 완료 조건**을 세운다. **자체 검증을 통과하기 전에는 "완료"라고 말하지 않는다** — 목적 1~3의 기준(`npm run verify:tokens`, `npm run verify:process`)이 그 검증이다. 일부가 막혔다면 나머지를 끝내고 무엇을 왜 남겼는지 명시한다.
- **위반 신호**: 검증 명령을 실행하지 않은 채 "완료"라고 보고한다. 실패한 검증 결과를 언급하지 않고 넘어간다.

---

## 표준 워크플로

모든 작업은 **Clarify → Reuse → Implement → Evaluate** 4단계를 거친다. 각 단계는 원칙 1~4와 1:1로 대응하며, 그 단계의 산출물이 원칙 이행의 증거가 된다.

| 단계 | 대응 원칙 | 이 단계에서 하는 일 | 산출물(증거) |
| --- | --- | --- | --- |
| 1 **Clarify** | 1 Think Before Coding | 모호한 지점을 질문으로 해소하고 Figma 원본을 확정한다 | Figma 원본 참조 |
| 2 **Reuse** | 2 Simplicity First | 기존 token·컴포넌트를 조사하고 대응을 적는다 | token 매핑 |
| 3 **Implement** | 3 Surgical Changes | 요청 범위만 구현한다 | `*.tsx` |
| 4 **Evaluate** | 4 Goal-Driven Execution | 완료 기준과 대조하고 자체 검증을 실행한다 | `*.stories.tsx` |

- **단계 건너뛰기 금지.** 특히 Reuse 없이 바로 만드는 것은 원칙 2 위반이고, Evaluate 없이 보고하는 것은 원칙 4 위반이다.
- 산출물 열은 목적 3의 4개 산출물과 **동일하다**. 즉 `npm run verify:process`는 워크플로를 거쳤는지를 그대로 판정한다 — 단계를 건너뛰면 그 단계의 산출물이 비어 exit code 1이 된다.

---

## 강제 레이어 (3중)

**계약: 위 4원칙은 문서 권고가 아니다.** 아래 3개 레이어가 같은 규칙을 서로 다른 층에서 강제한다.

| 레이어 | 무엇이 강제하는가 | 강제 방식 | 우회 가능성 | 상태 |
| --- | --- | --- | --- | --- |
| **1 선언** | `CLAUDE.md` (이 문서) | 원칙 · 규칙 · 완료 기준을 정의한다 | **높음** — 읽지 않으면 무효 | 구현됨 |
| **2 절차** | 에이전트 | Clarify → Reuse → Implement → Evaluate 순서로 원칙을 실행한다 | **중간** — 단계를 생략하면 산출물이 비어 `verify:process`가 잡는다 | 구현됨 — `.claude/agents/` 4개 + `.claude/skills/` 4개 |
| **3 자동차단** | hook | 토큰 외 값을 **도구 레벨**에서 차단한다 | **없음** — 파일에 기록되기 전에 막힌다 | 구현됨 — `.claude/hooks/check-hardcode.mjs` |

계약 조항:

1. **레이어 1 단독은 강제가 아니다.** 선언만 있는 규칙은 "권고"이며, 지켜지지 않아도 아무것도 막지 않는다. 따라서 이 문서에 규칙을 적는 것으로 목적 2가 pass되지 않는다.
2. **레이어 3이 최종 방어선이다.** 레이어 1(문서를 안 읽음)과 레이어 2(절차를 건너뜀)가 모두 실패해도, 토큰 외 시각 값은 파일에 도달하지 못한다.
3. **세 레이어는 대체 관계가 아니다.** 같은 규칙을 중복해서 막는다. "hook이 막아주니 원칙을 안 읽어도 된다"는 성립하지 않는다 — hook은 **시각 값만** 검사하며, 원칙 1(잘못된 가정) · 3(범위 밖 변경) · 4(미완 종료)는 검사하지 못한다. 이 3개는 레이어 1·2로만 강제된다.
4. **현재 상태**: 세 레이어 모두 파일로 존재한다(레이어 2 = `.claude/agents/` + `.claude/skills/`, 레이어 3 = `.claude/hooks/check-hardcode.mjs` + `.claude/settings.json`의 `PreToolUse`). hook이 검사하는 것은 시각 값뿐이므로 조항 3은 그대로 유효하다.
5. **레이어 3 활성화 조건**: Claude Code는 세션 시작 시점의 hook 설정을 사용한다. `.claude/settings.json`을 만들거나 고친 세션에서는 hook이 **발동하지 않는다** — 새 세션에서만 활성화된다. 활성 여부는 아래 판정 실험으로 확인한다. hook이 발동하지 않는 세션에서 목적 2는 **fail**이며, 그 사실을 완료 보고에서 숨기지 않는다.
   - 판정 실험: `.tsx` 파일에 `text-[#ff0000]`을 Write 시도 → 차단되면 레이어 3 활성(pass), 파일에 기록되면 비활성(fail).
   - hook 스크립트 자체의 정상 동작은 세션과 무관하게 확인할 수 있다: `npm run verify:tokens`.

---

## 토큰 규칙

목적 1이 "위반 0건이어야 pass"라는 **판정**이라면, 이 절은 "그래서 무엇을 써야 하는가"라는 **작성 규칙**이다.

- 모든 시각 값(color · spacing · radius · typography · border · shadow)은 **`src/tokens/`에 정의된 토큰만** 참조한다.
- **금지**: raw hex · `rgb()`/`rgba()`/`hsl()` · px/rem 리터럴 · Tailwind arbitrary value(`p-[13px]`, `text-[#ff0000]`) · 인라인 `style={{ }}`의 시각 값
- raw 값이 허용되는 **유일한 위치는 `src/tokens/` 내부**(토큰 정의부)다. 그 외 모든 경로에서 raw 값은 위반이다.
- 토큰에 없는 값이 필요하면 → 컴포넌트에 하드코딩하지 말고 **`src/tokens/`에 토큰을 추가한 뒤 참조한다.** 이것이 유일한 경로다.
- 토큰은 Tailwind v4 `@theme`로 노출해 유틸리티 클래스로 사용한다.
- 위반 시: 레이어 3 hook이 차단하고, `npm run verify:tokens`가 exit code 1을 낸다.

---

## 에이전트 라우팅

**에이전트 = 역할(누가 책임지는가) / 스킬 = 절차(어떻게 실행하는가).** 한 역할이 한 절차를 갖는다.

| 작업 | 에이전트 | 진입 스킬 | 편집 범위 |
| --- | --- | --- | --- |
| Figma 링크/노드 → 구현 | `figma-implementer` | `/figma-to-code` | 컴포넌트 · story |
| 하드코딩 감지 · 토큰 매핑 · Figma 변수 동기화 | `token-guardian` | `/sync-tokens` | **`src/tokens/**` 만** |
| Figma 없이 기존 토큰으로 생성/변형 | `component-builder` | `/new-component` | 컴포넌트 · story |
| 완료 전 검증 게이트 (PASS/FAIL) | `design-reviewer` | `/review-design` | **없음 — 읽기 전용** |

- **각 에이전트가 Clarify → Reuse → Implement → Evaluate 4단계를 전부 이행한다.** 한 에이전트가 한 단계만 담당하는 구조가 아니다 — 4단계는 모든 작업이 거치는 절차이고, 에이전트는 그 절차를 실행하는 역할 단위다.
- 라우팅 판별: Figma URL/노드가 있으면 `/figma-to-code`, 없으면 `/new-component`. 토큰이 부족해 막히면 `/sync-tokens`를 먼저 거친다. 어느 경로든 **완료 선언 전에 `/review-design`을 통과해야 한다.**
- 에이전트들은 Figma MCP 도구(`get_metadata` · `get_screenshot` · `get_design_context` · `get_variable_defs`)를 **직접 호출한다.** 중간 변환 레이어나 로컬 덤프를 두지 않는다 — 사본은 낡고, 낡은 사본은 디자인-코드 불일치의 출처다.

## 스킬 가이드

| 구분 | 정의 | 예 |
| --- | --- | --- |
| **에이전트** | **역할** — 어떤 판단을 누가 책임지는가 | `token-guardian`이 토큰 대응의 정확성을 책임진다 |
| **스킬** | **절차** — 그 역할을 어떤 순서로 호출하는가 | `/sync-tokens`가 `token-guardian`을 호출하는 절차 |

- 스킬 4개는 에이전트 4개와 **1:1 대응**한다: `/figma-to-code`→`figma-implementer` · `/sync-tokens`→`token-guardian` · `/new-component`→`component-builder` · `/review-design`→`design-reviewer`
- 각 스킬은 해당 에이전트를 **결정적으로** 호출한다. 스킬이 직접 구현하지 않는다 — 그래야 어떤 경로로 들어와도 같은 역할이 같은 제약 아래 일한다.
- 역할과 절차를 분리하는 이유: 절차가 바뀌어도 책임 주체는 그대로여서, 스킬만 고쳐도 라우팅 표는 유지된다.

---

## 명령어 계약

이 이름들은 확정된 계약이다.

| 명령어 | 무엇을 판정하는가 | 상태 |
| --- | --- | --- |
| `npm run verify:tokens` | 목적 1 · 2 | 사용 가능 — hook 스크립트의 `--scan` 모드 |
| `npm run verify:process` | 목적 3 | 사용 가능 — `.claude/scripts/verify-process.mjs` |
| `npm run typecheck` | 타입 안전성 | 사용 가능 |
| `npm run build-storybook` | 목적 3 보조 (story/autodocs 빌드 확인) | 사용 가능 |

미구현 명령어는 아직 실행하지 말 것 — 존재하지 않는다.

---

## 아직 없는 것 (뒤 청크에서 채움)

- **`src/components/Button.tsx`의 토큰 마이그레이션** — Tailwind 기본 팔레트(`bg-neutral-900`, `h-8`, `px-3`)를 쓰고 있다. hook은 이를 차단하지 않지만(검사 대상은 hex·rgb·raw px·대괄호 임의값) **토큰이 아니므로** `design-reviewer` 2번 항목에서는 FAIL이다.
- **Figma 연결 실증** — 아직 실제 Figma 파일로 `/figma-to-code`를 통과시킨 컴포넌트가 없다. MCP 4개 도구 호출 경로는 문서로만 정의된 상태다.

위 항목들은 모두 목적 1~3을 달성하기 위한 **수단**이다. 수단이 목적을 바꾸지 않는다.
